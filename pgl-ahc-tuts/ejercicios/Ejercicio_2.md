Ejercicio 2

Para cumplir con este ejercicio creé un "token.service" para realizar el guardado del token en teléfono devuelto por la api si lo devuelve, luego realicé la implementación de una función en authService llamada "loginUser" que envía un LoginRequest con usuario y contraseña a la api, con el endpoint y el verbo http y por último recive un Loginresponse también implementado como cuerpo para recibir además del id del usuario, el email y el token devuelto específicamente de ese usuario, esa función la utilizara el Login. Se terminó de implementar una pantalla de login funcional con dos inputs y un botón que laza una función, aquí se crea la función asíncrona OnLogin que se dispara al pulsar el botón de loguerase, primero valida que el email y la contraseña, si está bien, procede a mandar el email y el usuario en una función que guarda en un objeto res, la respuesta de la api se guarda en ese objeto y por último se intenta sacar el token del objeto tipo res, se saca llamando al atributo object y un interrogante para saber si existe on no, por último llamamos al atributo "token" del objeto "loginResponse" que es el token en concreto, si ha ocurrido un error y no hay token será undefined y no ocurrirá una excepción y por último si no hay token hay una última verificación que evita que no se continue sin token y termina en error al loguear el usuario, si hay token, el token es un tipo string que se guarda llamando a la función del "token.service" y se guarda en el teléfono.